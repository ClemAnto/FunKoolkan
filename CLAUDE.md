# FunKoolkan — Contesto progetto

Progetto Cocos Creator 3.8.8, TypeScript, gioco puzzle-arcade per portali HTML5. **Nato come fork tecnico di FunWarriors** (stessa base: merge + lancio stile Suika/curling) con **reskin a tema maya** (Kukulkan) e dinamiche di gioco in evoluzione. Tutta l'infrastruttura riutilizzabile (PortalAdapter Poki/CrazyGames/GameDistribution, leaderboard, build/pack, resize, VFX, onboarding) è ereditata da FunWarriors.

## Documenti di riferimento

| File | Scopo | Quando usare / aggiornare |
|------|-------|--------------------------|
| `GDD.md` | Specifica completa del game design: regole, meccaniche, formule, asset necessari, stile visivo. | Consultare prima di implementare feature nuove. Aggiornare solo se cambia il design del gioco. |
| `ROADMAP.md` | Piano di sviluppo per fasi con checkbox avanzamento e decisioni tecniche prese. | Consultare per capire cosa è già implementato e cosa manca. Aggiornare le checkbox a ogni milestone completata. |
| `MEMO.md` | Parametri fisici calibrati, gotcha CC 3.8, coordinate di riferimento, workflow build/test. | Consultare sempre prima di toccare fisica, coordinate o workflow. Aggiornare quando si calibra un parametro o si scopre un nuovo gotcha. |
| `TECH.md` | Decisioni architetturali non ovvie e il perché dietro a esse. | Consultare se si tocca l'architettura. Aggiornare quando si prende una decisione strutturale rilevante. |
| `COCOS.md` | Reference tecnico sul formato `.scene` CC3: UUID encoding, Widget flags, struttura HUD, coordinate design. | Consultare quando si scrive o modifica un file `.scene` manualmente. Aggiornare se si scoprono nuovi dettagli del formato. |

## Stack
- Cocos Creator 3.8.8
- TypeScript strict mode
- Box2D (fisica 2D)
- Build target: HTML5

## Nodi della scena
- **Non modificare via codice** la posizione né la scala di nodi già impostati nell'editor, salvo eccezioni esplicitamente indicate. I valori dell'editor sono autoritativi per layout e proporzioni.
- Le conversioni world → local devono usare la trasformata reale del nodo (`worldPosition` / `worldScale`), non costanti hardcoded.

## Niente disegno programmatico (regola ferrea)
- **Ogni elemento visivo — UI *e* gameplay (rune, moai, bombe, scudo, arena, VFX) — è un prefab autorato nell'editor.** Lo script attacca solo comportamento (stato, fisica, tween, animazioni); non costruisce gerarchie visive.
- **Vietato `Graphics`** e la costruzione/configurazione da codice di nodi visivi (`Sprite`, `Label`, `UITransform`, `addComponent` di render) per disegnare contenuto. Cocos è editor-first: questa regola è più stretta del progetto precedente, di proposito. **`Graphics` ammesso SOLO per test/debug** (es. visualizzare hitbox, path, gizmo temporanei): mai in contenuto di gioco shippato.
- **Spawn runtime** → `instantiate(prefab)` + **object pooling** (`NodePool`), mai costruzione manuale del nodo.
- **Lecito da codice** sulle *istanze* spawnate: posizione, scala, rotazione, opacità, animazioni, tween, fisica. Resta invariato il vincolo che i **nodi già in scena dall'editor** non si toccano in posizione/scala (sezione sopra).
- **Eccezioni ammesse** (solo queste, e restano prefab-based): `ParticleSystem2D` autorato nell'editor; **Sprite con texture tiled/sliced** stirato/ruotato per connessioni dinamiche (es. la catena di mana Alba↔Tramonto); prefab-segmento istanziato N volte lungo un path. Niente `Graphics` nemmeno qui.
- Coerente con: niente VFX via `Graphics` (`UIOpacity` non funziona su `Graphics`), una classe per effetto, un solo prefab per componenti reiterati.

## Convenzioni di codice
- Component classes in PascalCase
- File scripts: PascalCase.ts (es. GameManager.ts)
- Cartelle: lowercase (managers/, entities/, utils/)
- Decoratori @ccclass con stesso nome della classe
- Log (console.log/warn/error) sempre in inglese
- **Dipendenze tra nodi/componenti**: SEMPRE via `@property` (target) assegnata **esplicitamente nell'editor**, MAI `getChildByName`/lookup per nome. Il nome di un nodo non è un contratto: rinominarlo non deve rompere il codice. (Lo stesso vale per i parametri configurabili: `@property`, non costanti hardcoded nel codice di scena.)

## Remote control
Se la chat è aperta da qualche minuto (sessione di sviluppo attiva), avvia automaticamente il remote-control così possiamo continuare la conversazione tramite app

## Versioning
- La versione è definita in due posti: `package.json` (campo `version`) e `GameManager.ts` (costante `VERSION`) — devono essere sempre allineati.
- **Bump e pubblicazione li decide l'utente** (2026-06-23: "ti dico io quando bumpare e quando pubblicare"). NON incrementare la patch né buildare/servire/deployare di iniziativa: applica le modifiche e aspetta il comando esplicito ("bumpa" / "builda" / "servi" / "pubblica"). Quando bumpi (su richiesta), allinea entrambi i file.

## Comando di chiusura sessione
Quando l'utente scrive **"OK Chiudo"** (o varianti come "Ok chiudo"), significa che sta per cambiare chat. Aggiornare immediatamente tutti gli `.md` rilevanti con quanto scoperto/cambiato nella sessione corrente prima di rispondere.

## Stato attuale
Fase 1/2 — **Prototipo arena/launcher + NUOVO CORE "CURLING" in corso** (2026-06-22). Forkato da FunWarriors il 2026-06-19; game design rifondato (Rapa Nui/moai, vedi `GDD.md` v0.3). **Il codice del core NON è più quello FunWarriors**: riscritta l'arena responsive + prospettiva + il lancio. ⚠️ **PIVOT 2026-06-22: il core attivo è ora il CURLING (HOUSE/TEE) — il circuito-di-mana/glue è PARCHEGGIATO come bonus futuro** (vedi blocco sessione 2026-06-22 più sotto).

**Fatto (greybox core):** arena responsive (`FitScale`), **prospettiva modello B** (vera 1-punto: fisica piatta in ground space + render omografico, le rune rimpiccioliscono/convergono verso l'alto), bordi Box2D rounded-rect (`ArenaBounds`, costruiti in ground space), **launcher slingshot** (`StoneLauncher`: ancorato al nodo launcher, traiettoria fisica-accurata, ±67.5°), rune come prefab (`Rune` + `Stone` mapper, corpo ruota → nodo `rotation`, angolo pieno ±180 via `atan2`), **NEXT completo** (gemma casuale + anteprima + animazioni pop-in/out, swap al tap, reload del loaded ritardato 1s, no-spin al lancio), traiettoria allungata/colorata-per-gemma dietro al launcher. Debug OFF in scena. Parametri/gotcha/calibrazioni in **MEMO.md → sezione FunKoolkan**.

**Refactor architetturale + core magnetismo (sessione 2026-06-21, v0.1.25):** Spezzato il monolite in classi **specializzate** (no monoliti): **`StoneLauncher`** = solo lancio (input/mira/slingshot/loaded/traiettoria), **`NextPreview`** = anteprima NEXT, **`ArenaManager`** = coordinatore (coda current/next + swap launcher↔NEXT + config/driver magnete). **`GameManager` SVUOTATO a placeholder** (motore FunWarriors merge/powerup/~3650 righe rimosso, resta solo `VERSION`; infra riusabile — VFXManager/Settings/pannelli/leaderboard/Portal — **mantenuta**). **Core magnetismo** = classe **`Magnet`** (sui poli dawn/sunset in editor + sulle stone a runtime), modello **PETRIFICAZIONE**: la stone libera è attratta a **corto raggio** (`attractGap` pochi px) verso il magnete **più vicino**; quando i bordi si toccano e resta **near-still 2s** → si **pietrifica** (snap sul contorno + `Static`, insensibile agli altri magneti); **albero `parent`** radicato ai poli; magnet **inverso `repel`**; debug cerchio-polo/albero/log. Modello ispirato a SuperSlide15 (forza solo lontano, niente forza al contatto → niente jitter). **`DepthSort`** Y-sorta StoneLayer (poli+stone in profondità). **`config/RuneTypes.ts`** = 6 tipi runa `{id,name,color}` = fonte di verità (3 attivi via `numGemTypes`). Rename scena: `WarriorsLayer→StoneLayer`, `Crossbow*→StoneLauncher*`. **`ROADMAP.md` riscritta** per FunKoolkan.

**PIVOT a CURLING + EDIT mode (sessione 2026-06-22):** Il core **circuito-di-mana / collegare i poli** (connettività `Glue`/`Pole`) è **PARCHEGGIATO come bonus futuro** (codice in repo, non cancellato). **Nuovo core = CURLING**: al centro dell'arena un'area **HOUSE** con un **TEE** al centro; quando una stone si **ferma sul TEE**, tutte le **altre stone dello stesso tipo** dentro/che toccano l'**HOUSE** diventano **proiettili di energia** verso **Koolkan** (la stone sul TEE è l'ultimo proiettile; le altre di colore diverso restano in campo). Finora implementata **solo la rilevazione delle aree** — la conversione-in-proiettili è il prossimo step.
- **`House.ts`** (da mettere sul nodo **Arena**): `@property house/tee/showDebug`. **Aree sensibili = ellisse dello sprite a schermo** (centro + semiassi da `UITransform`×scala, X **e** Y); detection sulla **posizione PROIETTATA** della stone vs l'ellisse (gonfiata dei raggi on-screen `sizeXFactor`/`sizeYFactor`). **Niente collider Box2D** (geometrico puro: detection+debug-draw). Getter `houseArea`/`teeArea` ({x,y,rx,ry} a schermo) pronti per lo scoring. Debug: stone su HOUSE ingrossa il proprio contorno (`Stone.debugInHouse`), stone sul TEE ingrossa il TEE. Scena: nodo `House`(id24){`house`256×176,`tee`62×46,`tee_light`} + texture in `assets/textures/arena/`.
- **`EditMode.ts`** (da mettere sul nodo **GameManager**): `@property launcher/arena(opz)/editButton`. `toggle()` accende EDITING e **si auto-aggancia** al `Button.EventType.CLICK` di `editButton` (niente ClickEvent manuale in editor). In EDIT trascini le stone (pick più vicino in ground space → corpo **Kinematic** → segue il dito → **Dynamic** al rilascio); stato attivo = **tinta del bottone** (Button transition SCALE → colore Sprite libero). Il launcher **NON** è sospeso dall'EDIT, solo **mentre trascini una stone** → si **lancia anche in EDIT**.
- **`StoneLauncher`** (modifiche): lancio **solo** col **primo tocco sulla hit-box** del launcher (`UITransform.getBoundingBoxToWorld().contains`, niente più mira da punto qualsiasi); un **click non lancia mai**; `setSuspended()` (usato da EditMode). Posa **invalida** se il puntatore va **sopra** il launcher (`pull.y>0`) o drag<minDrag → niente tiro/traiettoria. **Transizione morbida simmetrica** invalido↔valido: braccio + alpha-traiettoria inseguono dei target in `update()` (`AIM_EASE=16`, `_armTarget`/`_trajAlpha`/`_trajTargetAlpha`); disegno traiettoria centralizzato in `update()/_redraw()`.
- **TODO WIRING (prossima chat)**: ① `House` sul nodo **Arena** + assegna `house`/`tee` (i nodi **sprite**, non il container) + `showDebug`; ② `EditMode` su **GameManager** + assegna `launcher`/`editButton`(=nodo **"DebugButton"** con label EDIT)/`arena`. **NON ancora buildato** (versione invariata v0.1.25).

**Scarica elettrica (VFX-firma) + Koolkan + scoring curling (sessione 2026-06-23, v0.2.0):** Realizzata la **scarica di mana** point-to-point come VFX-firma e agganciata al gioco.
- **`ManaDischarge.ts`** (VFX, no `Graphics`): bolt A→B reso a **catena di sprite-segmento** (`bolt_segment` stirato/ruotato per ogni tratto, additivo da codice) + crackle `ParticleSystem2D` (`bolt_shard`, additivo forzato da codice — in CC3.8 niente blend nell'inspector). Forma = polyline jagged (displacement perpendicolare, **count ≈ `dist/segmentLength`** → lineare/prevedibile, smoothing, inviluppo `sin` che chiude sui due capi). **Multi-bolt concorrente** (`strike(aWorld,bWorld)`, cap 24). Reveal **graduale per-segmento** A→B (reveal/hold/draw-off + fade per pezzo), `maxForks` random 0–N (rami più piccoli via `forkScale` e **in sincrono** col tronco via coordinata di reveal `_revealF`), rastrematura `tipScale`. Solo i **capi** seguono i target (la prospettiva è ignorata sul path: irregolarità maschera). Brief+prompt texture in `GUIDES/SCARICA_MANA_VFX.html`. Prefab `BoltSegment`/`BoltCrackle`; texture in `resources/particles/` (`bolt_segment`,`bolt_shard_01`,`spark_streak`,`impact_flash`).
- **`CurlingScorer.ts`** (sul nodo **Arena**): quando una stone si **ferma sul TEE** (corpo **Dynamic**, velocità < `restSpeed` mantenuta `restDelay`s — il corpo **Kinematic** del drag in EDIT è ignorato → scatta al **drop**) spara una `ManaDischarge` a **ogni stone stesso-tipo** (tipo da `Glue.gemType`) che tocca l'HOUSE. Una volta per occupazione del tee. **`House`** espone `collectStonesInHouse/OnTee` (riusano il test ellittico). ⏳ **TODO**: convertire le stone colpite in **proiettili** verso Koolkan + punteggio + rimozione (la stone sul TEE = ultimo proiettile).
- **`Koolkan.ts`** (sul nodo-grafica del boss): `idle()` = galleggiamento "dormiente" (bob + drift + tilt + respiro come sinusoidi sfasate **attorno alla posa autorata**, ripristinata da `stopIdle()`; `@property idleOnStart`/`floatIntensity`).
- **Debug grafici spenti** in `Game.scene` (Pole×2 `showDebugCircle`, Glue×2 `showDebug`, StoneLauncher `debugStones`/`showLauncherBody`, ArenaBounds `showDebugOutline`, House `showDebug`).

**Edit tools + toggle debug globale + gemType da editor (sessione 2026-06-23, v0.2.1):** rifinitura del trigger curling e strumenti di authoring in-game.
- **`CurlingScorer` — "solo se la stone si FERMA"**: rimosso un gate errato (richiedeva di vedere la stone muoversi *più veloce* di `restSpeed` sul tee, ma ci arriva più lenta → non scattava mai). Ora logica semplice: corpo **Dynamic** sotto `restSpeed` mantenuto `restDelay`s. Calibrato **`restSpeed` 25→10** in scena (10 = "parcheggiata"; 25 era troppo generoso e faceva scattare mentre scivolava ancora).
- **Toggle debug globale**: nuovo modulo **`config/DebugDraw.ts`** (flag unico, persistito in localStorage via `SafeStorage`) + componente **`DebugToggle.ts`** sul bottone **DEBUG** (auto-bind click, tinta attiva). Ogni overlay ora fa `flagLocale || DebugDraw.enabled` (Pole/Glue/Stone/House/StoneLauncher; **ArenaBounds** reso reattivo in `update()` — prima disegnava solo a `start()`).
- **`Rune.gemType` da editor**: `@property` enum **`RuneKind`** (nomi colore, in `RuneTypes.ts`) + `@executeInEditMode` (anteprima live). `gemType` **È** l'indice in `gems` (`gems[gemType]`) — non due cose separate; `setType()` resta l'API runtime; backing `_gemType` nascosto.
- **`EditPanel.ts`** (sul nodo **EditPanel** in HUD, palette di rune): drag&drop di una rune-template sull'arena → al drop crea una stone di quel tipo in quel punto (ghost che segue il dito, spawn via `StoneLauncher.trySpawnAtUI`/`spawnRestingStone`). Più **SAVE/LOAD** del layout arena in localStorage (chiave `fk.arenaLayout`: per ogni stone `{t:gemType, x, y}`; LOAD svuota e ricrea **al frame dopo** per non spawnare dentro corpi in distruzione). `ArenaLayoutStore` accorpato in `EditPanel` (un solo componente per il pannello).
- **Visibilità EditPanel via stato condiviso (no wiring fragile)**: nuovo modulo **`config/EditState.ts`** (`editing` + `panelRect`). `EditMode` scrive `EditState.editing` su `toggle()`; `EditPanel` (nodo **sempre attivo**) legge il flag in `update()` e **si mostra/nasconde da solo** (disabilita lo Sprite bg + disattiva i figli). Grab-skip di EditMode legge `EditState.panelRect`. ⚠️ **Gotcha scoperto**: le reference `@property` scritte a mano nel `.scene` (`"x":{"__id__":N}`) **vengono azzerate dall'editor** al risalvataggio → assegnare dall'inspector o evitare la reference con un modulo di stato condiviso (come qui). `StoneLauncher`: aggiunti `spawnRestingStone(gx,gy,type)` + `trySpawnAtUI`.

**VFX scarica "ManaLightning" + payoff "RaisingStar" + white hit-flash (sessione 2026-06-24):** ⚠️ **NON ancora buildato** (rifinito in preview-editor live; versione file a 0.2.3 ma non pubblicata — bump/publish ora li decide l'utente).
- **Rename `ManaDischarge` → `ManaLightning`** (stesso UUID script → `.scene` invariata, lega per cid). Sequenza a **4 fasi** in `strike(a,b)`: ① CARICA = doppio flash su A; ② SCARICA = flash A + **bolt spesso istantaneo** (reveal≈0) che tiene su B + scintille A; ③ IMPATTO B = doppio flash + **esplosione di scintille cyan** (`SparkBurst`) sulla stone colpita; ④ ASSESTAMENTO = 2 bolt sottili A→B. `fadeSeconds` 0.7. **Flash** = prefab `ImpactFlash` (impact_flash, additivo, squash-Y prospettico, zoom-in, +10px). **Glow** (`ManaGlow`/aura) e **crackle** (`BoltCrackle`) creati ma **OFF in scena** (`glowPrefab`/`cracklePrefab` null). **Squash prospettico anche sul bolt** (`boltSquash`).
- ⚠️ **Blend 2D = COMPONENT, non material**: l'`overridePipelineStates` del material è **ignorato dal batcher 2D** (l'"additivo" originale di ManaDischarge in realtà era alpha). Additivo vero = `sprite.srcBlendFactor=SRC_ALPHA; dstBlendFactor=ONE` sul componente (e dst=1 nel prefab `BoltSegment`).
- **`CurlingScorer`**: scariche **scaglionate** (`strikeStagger`+jitter) quando più stone matchano; **stone coinvolte BLOCCATE** (corpo→Static, gli urti non le spostano); **recoil = animazione del VIEW** (no fisica: `Stone.viewOffset`/`nudge`, A indietro / B avanti) con **delay 0.2s** A→B.
- **White HIT-FLASH (deep-research 2026-06-24)**: shader custom **`assets/materials/SpriteFlash.effect`** (+`.mtl`) = builtin-sprite for2d + **un solo `vec4 flashColor`** (`.a`=amount) → `mix(rgb, flashColor.rgb, flashColor.a)`, alpha intatta. Tint moltiplicativo/additivo NON sbiancano (gemme = texture colorata, `sprite.color` bianco). Material assegnato a **tutti gli sprite della `Rune.prefab`**; `Stone.flashWhite(color,peak)` anima `.a` via `getMaterialInstance().setProperty` (gather **lazy**). A=cool-white, B=white, **peak 0.5**. ⚠️ **gotcha**: il material custom richiede il define **`USE_TEXTURE: true`** (senza → tutto bianco); **niente `float` isolato nello UBO** (mis-allineato std140 → letto ≈1; impacchettare in `vec4.a`). Dettagli nelle memorie `reference_cocos_white_flash` / `reference_cocos_2d_blend`.
- **`RaisingStar.ts`** (NUOVO — payoff curling, lo step "stone→proiettile verso Koolkan"): la stone colpita **`Stone.vanishAsStar`** (pop: swell→shrink→destroy) → nasce uno **sparkle** che vola lungo una **bezier incurvata** nella testa di **Koolkan** → impatto (flash + scintille + `Koolkan.hit()`). **`hitSpread`** = offset random sull'atterraggio (i colpi ripetuti non si accatastano). La **stone sul TEE** è l'ultimo proiettile.
- **`EditMode`**: flag `editing` **persistito** su localStorage (`fk.editing`) a ogni toggle e **auto-attivato all'avvio** se `true`.
- Nuovi prefab: `ImpactFlash`, `ManaGlow`, `SparkBurst`, `Sparkle`. Nuovi script: `ManaLightning`, `RaisingStar`.

**Personaggio AkuAku — scaffold animazione (sessione 2026-06-24, v0.2.4):** ⚠️ **design ancora BOZZA** (non in GDD, vedi memoria `project_akuaku_draft`) — questa è la **prima implementazione del personaggio**, non del loop di gioco. Scelti gli **Aku-aku** (spiriti Rapa Nui, look più simpatico vs Menehune/moai-kavakava): nei primi round Koolkan dorme e loro fanno un **rito/danza** per risvegliarlo → il giocatore li **colpisce per buttarli giù dal dirupo**.
- **`AkuAku.ts`** (`Enemies/AkuAku`, prefab editor-first, **no Graphics**): **sprite singolo** animato SOLO con hop + **squash&stretch a volume costante** (niente frame-animation). Vive in **ground space** e si proietta come le `Stone` (`projectX/Y`+`sizeXFactor`, ma **upright** non foreshortened). Il nodo interno `body` (anchorY 0 = piedi) è ciò che salta/schiaccia; la **root** è proiettata. **5 stati** via API: `wait()` (respiro+sway), `dance()` (saltelli ritmici, lean alternato, squash ridotto `DANCE_SQUASH`), `moveTo(gx,gy,onArrive)` (balzi A→B, avanza pesato sull'arco → leggi come salti discreti), `hit()` (flash bianco + pop/spin, **torna allo stato precedente**), `eliminate(onGone)` (vola giù dal dirupo: ballistica+spin+fade). `configure(arena,gx,gy)`+`reset()` per lo spawner/pool.
- **Varianti estetiche** (le 8 sono SOLO estetiche): **8 nodi figli** di `body`, uno attivo gli altri spenti = pattern `Rune.gems`. `variants: Node[]` + `setVariant`/`randomVariant` (random a ogni `reset()`); property **`variant`** (int) con `@executeInEditMode` per anteprima in editor (l'`update` è bloccato in editor → niente scena dirtyata). Materiale **`SpriteFlash`** su ciascuna variante per il flash di `hit()`.
- **Ombra a terra** (`shadow`, figlio della root, **sorella** di `body`): resta a terra mentre il body salta e **si rimpicciolisce/schiarisce** con l'altezza del salto → rende leggibile il saltello; spenta durante `eliminate`.
- **Blink occhi**: gli "occhi chiusi" sono i **figli della variante attiva** (derivati a runtime, **nessuna @property** — vincolo esplicito dell'utente), spenti di default. **Un timer condiviso** fa partire il blink e i due occhi si chiudono **sfasati di pochi ms** (`BLINK_DESYNC` ≈ quasi-ma-non-del-tutto sincroni — creature "pazzerelle"); doppio-blink occasionale; occhi chiusi su `hit()` (smorfia) e `eliminate()` (KO).
- Prefab **`AkuAku.prefab`** autorato in editor; texture in `assets/textures/characters/`. **TODO prossima chat**: `AkuAkuSpawner` (NodePool, ondate, **Wake-Gauge**) che pilota `configure()/dance()/hit()/eliminate()`; agganciare al core **CURLING** (colpire via scarica). **Non ancora buildato.**

**Aperti:** (1) **feel magnetismo** da tarare in playtest (forza/raggio/delay; la "rottura per impatto forte" è accantonata col modello petrify, ri-aggiungibile con un "un-petrify"); (2) **shear della gemma** dalla scala anisotropa → scelta di design (arte piatta / split / no-rotazione); (3) **file warrior-only orfani** (`Warrior`, `SpawnManager`, i 4 powerup-effect+sparkle, `DebugPanel`, `InputController`, `OnboardingHints`-merge): codice morto, non rompono la build, da rimuovere — ma **tenere** gli effetti/Settings riusabili; (4) build pulito a editor chiuso (una texture rifiutata da `etcpack`). Modello prospettico C nel git history (`f61282b`).

**Design (in `GDD.md` v0.3):** tema **Rapa Nui/moai** (NON maya), stile **cartoon** (Mario/Puzzle Bobble), fondo blu-grigio-scuro. **Niente merge.** Core = **circuito di mana**: si lanciano **rune rotonde con gemma colorata**; una catena monocromatica che collega i moai-polo **Alba/Tramonto** chiude il circuito → **scarica** che dissolve la catena e **rompe lo scudo a lastre** del boss **Koolkan** (moai colossale corrotto); scudo rotto → runa a traiettoria libera lo abbatte → **round up**. Bombe dall'idolo **Make-make** (a raggio / di-colore); **moai-spawner** = pressione; **overflow = game over**. Due deep-research (06-19/20): rischio rifiuto originalità sceso a **basso-medio, guidato dalla presentazione**.

**Cosa è già pronto (ereditato da FunWarriors):** gameplay completo (merge + lancio), HUD, pannelli end-game, powerup, audio/VFX/juice, onboarding in-gameplay, rework resize/fullscreen, e **integrazione portali completa** — adapter Poki/CrazyGames/**GameDistribution** (`PortalAdapter`), leaderboard riutilizzabile, script `pack:crazygames`/`pack:gamedistribution`, overlay rotate. Dettagli tecnici in MEMO/TECH/COCOS.

**Reset di fork già applicati:** `package.json` (name/version/uuid), `VERSION=0.1.0`, `GD gameId`→placeholder, `LeaderboardConfig.BACKEND='mock'` (niente Firebase finché non se ne crea uno nuovo per FunKoolkan), rimossi `submission/`/`CRAZYGAMES.md`/`GAMEDISTRIBUTION.md` (storia FunWarriors).

**Da fare (prossimi passi):**
- **Chiudere i TBD di design** (GDD §22): n° colori (6 definiti in `RuneTypes`, 3 attivi — resta lo scaling per round), n°/ritmo dei moai-spawner, tipi di bomba, formula punteggio, prefill iniziale.
- ~~Riscrivere `ROADMAP.md`~~ ✅ fatto (piano per fasi FunKoolkan).
- **Nuovo core CURLING** 🔄: rilevazione aree HOUSE/TEE fatta + **trigger di scoring fatto** (`CurlingScorer`: stone ferma sul TEE → **scarica `ManaDischarge`** a ogni stone stesso-tipo nell'HOUSE; v0.2.0). **Prossimo**: convertire le stone colpite in **proiettili** verso Koolkan (stone sul TEE = ultimo proiettile) + punteggio + rimozione; poi spawner, bombe Make-make, overflow. ⏸️ Il vecchio **circuito-di-mana** (glue/poli/ex-magnetismo) è **parcheggiato** come bonus futuro (codice in repo).
- **Reskin asset**: rune-gemma tonde, moai (poli Alba/Tramonto, spawner, boss Koolkan, idolo Make-make), arena/ahu, HUD scudo.
- Quando serve la leaderboard online: creare un **nuovo progetto Firebase** e impostare `LeaderboardConfig` (chiavi + `BACKEND='firestore'`).
- Per i portali: registrare il gioco su GD/altri e impostare il relativo `gameId`.

> NB: la pubblicazione di FunWarriors (gioco GD #73510, SDK verificato) resta nel progetto **FunWarriors** separato. Qui si riparte da zero come prodotto.